# Server
PORT=1323
# For Docker: set to /data/config-hub.db (volume mount)
# For local: set to config-hub.db or leave commented
DB_PATH=config-hub.db

# JWT Secret (auto-generated random 24-hex if unset)
JWT_SECRET=

# Disable new user registration (set to "false" to disable)
ENABLE_REGISTRATION=false
